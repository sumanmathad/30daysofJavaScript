
//Create datatypes.js file and use the JavaScript typeof operator to check different data types. 
//Check the data type of each variable

let alpha = "a";
let line = "this is a line";
let num = 0;
let isWorking = true;
let nothing = null;
let notinitialised;
let empty ='';

console.log(typeof nothing, typeof empty, typeof notinitialised, typeof alpha, typeof line, typeof num, typeof isWorking);